maubot: 0.5.0
id: net.codestorm.csreg-scanner
version: 0.2.1
license: MIT
modules:
  - csreg_scanner
main_class: CSRegScanner

database: true
database_type: asyncpg
config: true
webapp: true

dependencies:
  - asyncpg>=0.29
  - aiohttp>=3.10
  - dnspython>=2.6
extra_files:
  - base-config.yaml
  # Web based config value calculator
  - csreg_scanner/web/calc.html
  # Vendored Public Suffix List (MPL 2.0) for the per-eTLD+1 ban cap.
  - csreg_scanner/public_suffix_list.dat
  